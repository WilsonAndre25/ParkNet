@page
@model ParkNet.Pages.My.AvencaBookings.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.AvencaBooking[0].TypeBalance)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AvencaBooking[0].EntryDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AvencaBooking[0].ExitDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AvencaBooking[0].User)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AvencaBooking[0].ParkingSpot)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AvencaBooking[0].ParkingLot)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.AvencaBooking) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.TypeBalance)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.EntryDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ExitDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.User.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ParkingSpot.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ParkingLot.Name)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
